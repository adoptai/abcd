"""Shared library for the agent-harness debug-loop CLI scripts (cli/harness_*.py).

Parallel to cli/wdl_common/, but talks to adoptwebui's end-user agent-harness
API (Bearer JWT) instead of the WDL actions API.
"""
